// NOTE: This is unused for the moment
import { writable } from 'svelte/store';

export const orbitControlsEnabled = writable(true);
export const createMode = writable(false);
export const editMode = writable(false)
